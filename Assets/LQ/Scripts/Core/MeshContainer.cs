using UnityEngine;

namespace LQ {
    /// <summary>Holds generated meshes as sub-assets (runtime assembly so Android builds keep the script reference).</summary>
    public class MeshContainer : ScriptableObject { }
}
